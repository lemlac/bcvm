# bvcm
A stack-based virtual machine written in portable C11, architecturally in the same family as the JVM or the CLR: a compact instruction set operating over an explicit value stack, per-call stack frames, a runtime constant pool, a managed heap with a real mark-sweep garbage collector, and a disassembler for inspecting compiled bytecode.
